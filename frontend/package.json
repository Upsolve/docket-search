{
  "name": "my_docket",
  "version": "0.0.1",
  "description": "Simple UI for bankruptcy filers to get their case docket entries.",
  "scripts": {
    "dev": "parcel ./src/index.html --out-dir ./dist",
    "build": "rimraf dist && parcel build ./src/*.html ./src/**/*.html --out-dir ./dist",
    "start": "npm run dev"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "axios": "^0.18.1",
    "lodash": "^4.17.15"
  }
}
