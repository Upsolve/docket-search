{
  "name": "my_docket_backend",
  "version": "0.0.1",
  "description": "Simple backend sitting between MyDocket frontend and mock PACER API.",
  "scripts": {
    "start:local": "node ./src/start",
    "start": "nodemon -L --exec npm run start:local"
  },
  "devDependencies": {
    "nodemon": "^1.18.10"
  },
  "dependencies": {}
}
